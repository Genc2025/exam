name: Deploy Miranda
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: github-pages-miranda
  cancel-in-progress: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout exact commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1
      - name: Verify Miranda Intelligence V18
        run: |
          set -euo pipefail
          grep -q 'nutrition.js?v=18' index.html
          grep -q 'family.js?v=18' index.html
          grep -q 'family-premium.js?v=18' index.html
          grep -q 'family.css?v=18' index.html
          grep -q 'premium.js?v=18' index.html
          grep -q 'maxProfiles:6' family.js
          grep -q 'PLANI IM SOT' family.js
          grep -q 'SMART MEALS' family-premium.js
          grep -q 'SMART GROCERY' family-premium.js
          grep -q 'FAMILY BOARD' family-premium.js
          grep -q 'miranda_usda_complete.sqlite' nutrition.js
          test -s miranda_usda_complete.sqlite
          test -f fresh.css
          test -f fresh.js
          test -f diet.js
          test -f recipes150.js
          test -f ultra.css
          test -f ultra.js
          test -f premium.css
          test -f premium.js
          test -f nutrition.css
          test -f nutrition.js
          test -f family.css
          test -f family.js
          test -f family-premium.js
          test -f manifest.webmanifest
          test -f sw.js
      - name: Build isolated site
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir _site
          cp index.html fresh.css fresh.js diet.js recipes150.js ultra.css ultra.js premium.css premium.js nutrition.css nutrition.js family.css family.js family-premium.js miranda_usda_complete.sqlite manifest.webmanifest sw.js _site/
          touch _site/.nojekyll
          printf 'MIRANDA-INTELLIGENCE-V18 %s\n' "$GITHUB_SHA" > _site/build.txt
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload isolated site
        uses: actions/upload-pages-artifact@v3
        with:
          name: miranda-pages-${{ github.run_id }}-${{ github.run_attempt }}
          path: _site
      - name: Deploy Miranda
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: miranda-pages-${{ github.run_id }}-${{ github.run_attempt }}
