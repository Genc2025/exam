/* Miranda recipe photography policy: never substitute random or keyword-matched images for a recipe. */
function babyPhotoTag(r){
  if(!r||!r.image||r.image.status!=='VERIFIED'||!r.image.localPath)return '';
  return `<img src="${r.image.localPath}" alt="${r.title}" loading="lazy" decoding="async" onerror="this.remove()">`;
}
recipeCard=function(r,i){return `<article class="recipe-card reveal"><div class="food-art meal-photo art-${i%6}">${babyPhotoTag(r)}<span class="photo-fallback">${r.icon}</span><small>${r.age}</small></div><div class="recipe-copy"><div class="meta"><span>${r.cat}</span><span>•</span><span>${r.time}</span></div><h3>${r.title}</h3><p>${r.desc}</p><button onclick="openRecipe(${i})">Shiko recetën <b>→</b></button></div></article>`}
openRecipe=function(i){const r=recipes[i];app.innerHTML=`<div class="recipe-page"><button class="back" onclick="home()">← Kthehu te libri</button><div class="recipe-detail reveal"><div class="detail-art meal-photo art-${i%6}">${babyPhotoTag(r)}<span class="photo-fallback">${r.icon}</span><small>${r.age}</small></div><div class="detail-copy"><p class="eyebrow">${r.cat} · ${r.age} · ${r.time}</p><h1>${r.title}</h1><p class="detail-intro">${r.desc}</p><div class="quick-facts"><div><small>PORCIONI</small><b>${r.portion}</b></div><div><small>TEKSTURA</small><b>${r.texture}</b></div></div><section class="detail-section"><h3>Përbërësit</h3><ul>${r.ingredients.map(x=>`<li>${x}</li>`).join('')}</ul></section><section class="detail-section"><h3>Përgatitja hap pas hapi</h3><ol>${r.steps.map(x=>`<li>${x}</li>`).join('')}</ol></section><section class="detail-section info-block"><h3>Si ta shërbesh</h3><p>${r.serve}</p></section><section class="detail-section info-block"><h3>Alergjenët</h3><p>${r.allergens}</p></section><section class="detail-section info-block"><h3>Ruajtja</h3><p>${r.storage}</p></section><section class="detail-section warning-block"><h3>Çfarë të shmangësh</h3><p>${r.avoid}</p></section><div class="safety"><b>Shënim sigurie:</b> Mosha është orientuese. Përshtate gjithmonë madhësinë dhe teksturën me aftësitë e bebit, kontrollo temperaturën dhe mbikëqyre gjatë gjithë ngrënies.</div></div></div></div>`;scrollTo(0,0);initReveal()}
